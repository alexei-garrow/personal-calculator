# To-Do List

This is a list of potential features and improvements for the calculator. For a detailed breakdown of the different calculation methods, please see `methods.md`.

## Core Functionality

- [✅] Explore the `Function` constructor method.
- [ ] Implement a robust evaluation method (e.g., using the `math.js` library).
- [ ] Implement a manual parser that respects the order of operations (as a learning exercise).
- [ ] Add order of operations (BEDMAS) support.

## Features

- [ ] Add a history cache to track and display previous calculations.
- [ ] Show the ongoing calculation dynamically above the result.
- [ ] Implement an "Answer" (Ans) button to use the last result.
- [✅] Implement a "Backspace" (Del) button to delete the last character.

## UI/UX

- [ ] Add more advanced styling or animations.
- [ ] Refine the UI further (adjust button sizes, fonts, colors).

## Learning Exercises

- [ ] Implement a custom `slice()` function to understand its underlying mechanics.

## Workflow Suggestions

- [ ] Review code using the `review-code` workflow.
- [ ] Reflect on learning using the `reflect` workflow.

Option A — Add operator precedence (BEDMAS)
A two‑pass evaluator is a great next step.
Option B — Add parentheses
This introduces recursion in a gentle way.
Option D — Add keyboard support
Makes the calculator feel real.
